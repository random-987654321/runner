name: calculate
description: "Evaluate a mathematical expression."
parameters:
  type: object
  properties:
    expr:
      type: string
  required: ["expr"]
mcp_server: "python3"
server_args: "mcp_calculate.py"
port: 50054
