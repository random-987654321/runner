name: Queen Orchestrator
on:
  workflow_dispatch:
permissions:
  contents: write
  actions: write
env:
  MAX_WORKERS: 19
  OLLAMA_MODEL: granite4:tiny-h
  HERMES_DASHBOARD_PORT: 9119
jobs:
  queen:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      
      - name: Install System Dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl python3-pip python3-venv netcat-openbsd openssh-server
          curl -L -o cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared
          
      - name: Setup Ollama Engine
        uses: ai-action/setup-ollama@v2
        
      - name: Pull Ollama Model Weights
        run: |
          ollama pull ${{ env.OLLAMA_MODEL }}
          
      - name: Verify Ollama is Ready & Model Works
        run: |
          echo "⏳ Waiting for Ollama API..."
          for i in {1..30}; do
            if nc -z 127.0.0.1 11434; then
              echo "✅ Ollama is online!"
              break
            fi
            sleep 2
          done
          echo "🧠 Testing model inference..."
          ollama run ${{ env.OLLAMA_MODEL }} "Say hello" 2>&1 | head -5

      - name: Install Official Hermes Agent
        run: |
          python3 -m pip install --upgrade pip
          pip install "hermes-agent[web,pty]"
          
      # NEW STEP: This will print a list of all available commands (serve, api, gateway, etc.)
      - name: Discover Hermes Commands
        run: |
          echo "==============================================="
          echo "Below are all available Hermes CLI commands:"
          echo "==============================================="
          hermes --help
          
      - name: Pre-configure Hermes Runtime Environment
        run: |
          mkdir -p ~/.hermes
          cat << 'EOF' > ~/.hermes/config.yaml
          provider: "ollama"
          model: "granite4:tiny-h"
          providers:
            ollama:
              base_url: "http://127.0.0.1:11434"
          EOF
          
      - name: Start Hermes Services
        env:
          HERMES_ALLOWED_ORIGINS: "*"
          HERMES_ALLOWED_HOSTS: "*"
          HERMES_DISABLE_CSRF: "true"
          HERMES_INSECURE: "true"
        run: |
          # Attempting to start the backend gateway alongside the dashboard
          # If 'serve' is wrong, it will error in the logs, helping us find the right word
          nohup hermes serve > hermes_backend.log 2>&1 &
          sleep 2
          
          nohup hermes dashboard \
            --host 127.0.0.1 \
            --port ${{ env.HERMES_DASHBOARD_PORT }} \
            --no-open \
            --skip-build > hermes_dashboard.log 2>&1 &
          
          echo "⏳ Waiting for dashboard to bind..."
          for i in {1..30}; do
            if nc -z 127.0.0.1 ${{ env.HERMES_DASHBOARD_PORT }}; then
              echo "✅ Dashboard is online!"
              break
            fi
            sleep 3
          done
          
      - name: Start Cloudflare Tunnel Connection
        id: tunnel
        run: |
          nohup ./cloudflared tunnel \
            --url http://127.0.0.1:${{ env.HERMES_DASHBOARD_PORT }} \
            --http-host-header "127.0.0.1:${{ env.HERMES_DASHBOARD_PORT }}" > tunnel.log 2>&1 &
          
          sleep 2
          
          echo "⏳ Catching TryCloudflare edge address registration..."
          for i in {1..20}; do
            URL=$(grep -oP 'https://[a-zA-Z0-9-]+\.trycloudflare\.com' tunnel.log | head -1)
            if [ -n "$URL" ]; then 
              echo "✅ Tunnel successfully established!"
              echo "tunnel_url=$URL" >> $GITHUB_OUTPUT
              break
            fi
            echo "Waiting for Cloudflare edge... (Attempt $i/20)" 
            sleep 3
          done
          
      - name: Print Orchestration Access Route & Keep Alive
        run: |
          echo "================================================================="
          echo "🌐 OFFICIAL HERMES CONTROL PLANE PORTAL READY:"
          echo "👉 ${{ steps.tunnel.outputs.tunnel_url }}"
          echo "================================================================="
          sleep infinity
