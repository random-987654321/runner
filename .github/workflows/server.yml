name: Server

on:
  workflow_dispatch:

permissions:
  actions: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      # ----- Caching: Ollama Models -----
      - uses: actions/cache@v4
        id: ollama
        with:
          path: ~/.ollama/models
          key: ollama-micro-distilled
          restore-keys: ollama-

      # ----- Caching: Cloudflared -----
      - uses: actions/cache@v4
        id: cloudflared
        with:
          path: ./cloudflared
          key: cloudflared
          restore-keys: cloudflared-

      # ----- Setup Ollama (only on cache miss) -----
      - if: steps.ollama.outputs.cache-hit != 'true'
        uses: ai-action/setup-ollama@v2

      # ----- Pull the distilled micro model (only on cache miss) -----
      - if: steps.ollama.outputs.cache-hit != 'true'
        run: ollama pull hf.co/glogwa68/granite-4.0-h-micro-DISTILL-OPUS-4.5-think-GGUF:Q4_K_M

      # ----- Save Ollama cache (only on cache miss) -----
      - if: steps.ollama.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ~/.ollama/models
          key: ollama-micro-distilled

      # ----- Download Cloudflared (only on cache miss) -----
      - if: steps.cloudflared.outputs.cache-hit != 'true'
        run: |
          curl -Lo cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared

      # ----- Save Cloudflared cache (only on cache miss) -----
      - if: steps.cloudflared.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ./cloudflared
          key: cloudflared

      # ----- Main: Run Ollama + Open WebUI + Tunnel + Self‑Replication -----
      - name: Run and Replicate
        env:
          GH_TOKEN: ${{ secrets.TOKEN }}
        run: |
          set -e

          # ---------- Explicit Ollama Optimisations ----------
          export OLLAMA_HOST=0.0.0.0:11434
          export OLLAMA_NUM_PARALLEL=2
          export OLLAMA_MAX_LOADED_MODELS=1
          export OLLAMA_KEEP_ALIVE=10m
          export OLLAMA_CONTEXT_LENGTH=4096
          export OLLAMA_FLASH_ATTENTION=true
          export OLLAMA_SCHED_SPREAD=true

          echo "🚀 Starting Ollama with optimised settings..."
          echo "  - OLLAMA_HOST=${OLLAMA_HOST}"
          echo "  - OLLAMA_NUM_PARALLEL=${OLLAMA_NUM_PARALLEL}"
          echo "  - OLLAMA_CONTEXT_LENGTH=${OLLAMA_CONTEXT_LENGTH}"

          pkill -f ollama 2>/dev/null || true
          ollama serve &

          # Wait for Ollama to be ready
          for i in {1..20}; do
            if curl -s http://localhost:11434/api/tags > /dev/null; then
              echo "✅ Ollama ready"
              break
            fi
            sleep 2
          done
          if ! curl -s http://localhost:11434/api/tags > /dev/null; then
            echo "❌ Ollama failed to start"
            ps aux | grep ollama || true
            exit 1
          fi

          # Pull model (idempotent – does nothing if already present)
          ollama pull hf.co/glogwa68/granite-4.0-h-micro-DISTILL-OPUS-4.5-think-GGUF:Q4_K_M

          # ----- Install Open WebUI (Python) -----
          echo "🐍 Installing Open WebUI..."
          sudo apt-get update -qq
          sudo apt-get install -y python3-pip python3-venv
          python3 -m venv openwebui-env
          source openwebui-env/bin/activate
          pip install --quiet open-webui

          echo "🌐 Starting Open WebUI on port 3000..."
          open-webui serve --host 0.0.0.0 --port 3000 &

          # Wait for Open WebUI to be ready
          for i in {1..10}; do
            if curl -s http://localhost:3000 > /dev/null; then
              echo "✅ Open WebUI ready"
              break
            fi
            sleep 2
          done
          if ! curl -s http://localhost:3000 > /dev/null; then
            echo "❌ Open WebUI failed to start"
            exit 1
          fi

          # ----- Start Cloudflare Tunnel (→ port 3000) -----
          echo "🌐 Starting Cloudflare tunnel..."
          if [ ! -f ./cloudflared ]; then
            curl -Lo cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
            chmod +x cloudflared
          fi
          pkill -f cloudflared 2>/dev/null || true
          ./cloudflared tunnel --protocol http2 --url http://localhost:3000 2>&1 | tee tunnel.log &

          # Extract tunnel URL
          for i in {1..30}; do
            TUNNEL_URL=$(grep -oP 'https://[a-zA-Z0-9-]+\.trycloudflare\.com' tunnel.log | head -1)
            if [ -n "$TUNNEL_URL" ]; then
              echo "✅ Tunnel URL: ${TUNNEL_URL}"
              break
            fi
            sleep 2
          done
          if [ -z "$TUNNEL_URL" ]; then
            echo "❌ Failed to get tunnel URL"
            tail -20 tunnel.log
            exit 1
          fi
          echo "::notice::Tunnel URL: ${TUNNEL_URL}"

          # ----- Update config.json in the chat repo -----
          echo "📝 Updating frontend config..."
          CHAT_REPO="${{ github.repository_owner }}/chat"
          NEW_CONFIG=$(jq -n --arg url "$TUNNEL_URL" '{apiUrl: $url}')
          ENCODED=$(echo "$NEW_CONFIG" | base64 -w0)

          FILE_INFO=$(curl -s -H "Authorization: Bearer ${{ secrets.TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${CHAT_REPO}/contents/config.json")
          SHA=$(echo "$FILE_INFO" | jq -r '.sha')

          if [ "$SHA" != "null" ]; then
            PAYLOAD=$(jq -n \
              --arg msg "update API URL" \
              --arg content "$ENCODED" \
              --arg sha "$SHA" \
              --arg name "github-actions[bot]" \
              --arg email "github-actions[bot]@users.noreply.github.com" \
              '{message: $msg, content: $content, sha: $sha, author: {name: $name, email: $email}, committer: {name: $name, email: $email}}')
          else
            PAYLOAD=$(jq -n \
              --arg msg "add API URL" \
              --arg content "$ENCODED" \
              --arg name "github-actions[bot]" \
              --arg email "github-actions[bot]@users.noreply.github.com" \
              '{message: $msg, content: $content, author: {name: $name, email: $email}, committer: {name: $name, email: $email}}')
          fi

          curl -s -X PUT -H "Authorization: Bearer ${{ secrets.TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${CHAT_REPO}/contents/config.json" \
            -d "$PAYLOAD"

          echo "::notice::Frontend URL: https://${{ github.repository_owner }}.github.io/chat/"

          # ----- Self‑replication loop -----
          echo "🔄 Entering self‑replication loop..."
          START_TIME=$(date +%s)
          TRIGGER_AT=$((350 * 60))
          TRIGGERED=false

          while true; do
            ELAPSED=$(( $(date +%s) - START_TIME ))

            if [ $ELAPSED -ge $TRIGGER_AT ] && [ "$TRIGGERED" = "false" ]; then
              echo "⏰ Triggering replacement workflow..."
              curl -X POST -H "Authorization: Bearer ${{ secrets.TOKEN }}" \
                -H "Accept: application/vnd.github.v3+json" \
                "https://api.github.com/repos/${{ github.repository }}/actions/workflows/server.yml/dispatches" \
                -d '{"ref":"${{ github.ref }}"}'
              TRIGGERED=true
            fi

            # Health checks
            if ! curl -s http://localhost:11434/api/tags > /dev/null; then
              echo "⚠️ Ollama down – restarting..."
              pkill -f ollama 2>/dev/null || true
              ollama serve &
            fi
            if ! pgrep -f cloudflared > /dev/null; then
              echo "⚠️ Tunnel down – restarting..."
              ./cloudflared tunnel --protocol http2 --url http://localhost:3000 2>&1 | tee -a tunnel.log &
            fi
            if ! curl -s http://localhost:3000 > /dev/null; then
              echo "⚠️ Open WebUI down – restarting..."
              pkill -f open-webui 2>/dev/null || true
              source openwebui-env/bin/activate
              open-webui serve --host 0.0.0.0 --port 3000 &
            fi

            sleep 30
          done
