{
  "models": {
    "providers": {
      "ollama": {
        "baseUrl": "http://localhost:11434",
        "models": [
          {
            "id": "granite4:tiny-h",
            "api": "ollama"
          }
        ]
      }
    }
  },
  "agents": {
    "defaults": {
      "model": {
        "primary": "ollama/granite4:tiny-h"
      },
      "systemPrompt": "You are a helpful assistant. When a user asks a question that requires research, summarization, data processing, or any non-trivial task, use the 'run_worker' tool. Always delegate complex tasks to workers. Never try to answer complex questions yourself.",
      "tools": ["run_worker"]
    }
  },
  "tools": {
    "run_worker": {
      "type": "command",
      "command": "__SCRIPT_DIR__/worker_launcher.sh",
      "description": "Execute a query in a temporary GitHub Actions worker. Returns the result.",
      "parameters": {
        "query": {
          "type": "string",
          "description": "The exact user query to be executed"
        }
      }
    }
  }
}
